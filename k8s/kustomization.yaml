apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kcirtap

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  # - ingress.yaml        # Disabled - using Cloudflare Tunnel instead
  # - cloudflare-tunnel.yaml  # Tunnel managed by Helm chart (cloudflare-tunnel-remote) — config in Cloudflare dashboard

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
